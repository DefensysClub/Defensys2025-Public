# Required sections
name: "coin flip"
author: "WhyDude230"
category: crypto
description: AES ECB Mode is not secure, so here is another strong , unbreakable and  military grade mode of encryption, see if you can defeat it

value: 500
type: dynamic

extra:
     initial: 500
     decay: 100
     minimum: 50

# ----------- do not touch those -------
image: null
protocol: null
host: null
connection_info: <ip>:4001
# --------------------------------------

flags:
    # A static case sensitive flag
    - Defensys{seems_like_you_know_your_things_keep_it_up_89131312312}

# Tags are used to provide additional public tagging to a challenge
# Can be removed if unused
# Accepts strings
tags:
    - crypto

# Provide paths to files from the same directory that this file is in
# Accepts strings
files:
    - dist/chall.py

# The state of the challenge.
# If the field is omitted, the challenge is visible by default.
# If provided, the field can take one of two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"